{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main, HUD, and onboarding windows",
  "windows": ["main", "hud", "onboarding"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default",
    "process:allow-restart",
    "notification:default"
  ]
}
